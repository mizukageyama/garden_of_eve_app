import 'package:flutter/material.dart';

class ProductTileRect extends StatelessWidget {
  const ProductTileRect({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
